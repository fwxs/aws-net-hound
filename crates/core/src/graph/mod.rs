//! Graph node/edge model produced by `crate::ingest::map::topology` and
//! consumed by a future `GraphWriter` implementation — see
//! `crates/core/docs/schema.md` for the authoritative node/edge shapes.

pub mod model;
